# geogrid 人工验证指南

本文只描述后续验证条件。本次工作没有编译或运行 WPS。

## 1. 激活 `.copy` 文件前

需要先完成两类源码修正：

1. 把 WPS `constants_module.F`、`module_map_utils.F` 及相关 metgrid 常数中的地球半径改为 MarsWRF 使用的火星半径。

| 文件 | 修改内容 |
|---|---|
| [geogrid/constants_module.F](/home/zy/WRF/marswrf/WPS/geogrid/src/constants_module.F:25) | 地球半径改为火星半径 |
| [geogrid/module_map_utils.F](/home/zy/WRF/marswrf/WPS/geogrid/src/module_map_utils.F:178) | 投影默认使用火星半径 |
| [geogrid/gridinfo_module.F](/home/zy/WRF/marswrf/WPS/geogrid/src/gridinfo_module.F:427) | 全球/区域网格距离按火星半径计算 |
| [metgrid/constants_module.F](/home/zy/WRF/marswrf/WPS/metgrid/src/constants_module.F:25) | 地球半径改为火星半径 |
| [metgrid/module_map_utils.F](/home/zy/WRF/marswrf/WPS/metgrid/src/module_map_utils.F:178) | 投影默认使用火星半径 |
| [metgrid/read_met_module.F](/home/zy/WRF/marswrf/WPS/metgrid/src/read_met_module.F:143) | intermediate 缺少半径时回退到 3389.92 km |
| [metgrid/module_date_pack.F](/home/zy/WRF/marswrf/WPS/metgrid/src/module_date_pack.F:16) | 五位 sol 解析、669-sol 日期差、跨年和输出格式 |



./geogrid/src/constants_module.F 修改内容：
```fortran
   ! origin codes
   real, parameter :: EARTH_RADIUS_M = 6370000.   ! same as MM5 system
   real, parameter :: EARTH_CIRC_M = 2.*PI*EARTH_RADIUS_M
   ! MarsWRF spherical radius; keep in sync with WRFV3/share/module_model_constants.F.
   real, parameter :: MARS_RADIUS_M = 3389.92e03
   real, parameter :: MARS_CIRC_M = 2.*PI*MARS_RADIUS_M
```
./geogrid/src/gridinfo_module.F 修改内容
```fortran
427            dxkm = EARTH_RADIUS_M * PI * 2.0 / (e_we(1)-s_we(1))
428            dykm = EARTH_RADIUS_M * PI       / (e_sn(1)-s_sn(1))
            dxkm = MARS_RADIUS_M * PI * 2.0 / (e_we(1)-s_we(1))
            dykm = MARS_RADIUS_M * PI       / (e_sn(1)-s_sn(1))
         ! If dx,dy specified, however, assume regional grid
         else
            dlatdeg = dy
            dlondeg = dx
434            dxkm = dlondeg * EARTH_RADIUS_M * PI * 2.0 / 360.0
435            dykm = dlatdeg * EARTH_RADIUS_M * PI * 2.0 / 360.0
            dxkm = dlondeg * MARS_RADIUS_M * PI * 2.0 / 360.0
            dykm = dlatdeg * MARS_RADIUS_M * PI * 2.0 / 360.0
```

./geogrid/src/module_map_utils.F 修改内容
```fortran
178      REAL             :: rebydx   ! Earth radius divided by dx
178      REAL             :: rebydx   ! Spherical planet radius divided by dx
         REAL             :: knowni   ! X-location of known lat/lon
         REAL             :: knownj   ! Y-location of known lat/lon
181      REAL             :: re_m     ! Radius of spherical earth, meters
181      REAL             :: re_m     ! Radius of spherical planet, meters
...
231      proj%re_m     = EARTH_RADIUS_M
231      proj%re_m     = MARS_RADIUS_M
```

./metgrid/src/constants_module.F 修改内容：
```fortran
25   real, parameter :: EARTH_RADIUS_M = 6370000.   ! same as MM5 system
26   real, parameter :: EARTH_CIRC_M = 2.*PI*EARTH_RADIUS_M

   ! MarsWRF spherical radius; keep in sync with WRFV3/share/module_model_constants.F.
   real, parameter :: MARS_RADIUS_M = 3389.92e03
   real, parameter :: MARS_CIRC_M = 2.*PI*MARS_RADIUS_M
```

./metgrid/src/module_map_utils.F 修改内容：
```fortran
178      REAL             :: rebydx   ! Earth radius divided by dx
178      REAL             :: rebydx   ! Spherical planet radius divided by dx
      REAL             :: knowni   ! X-location of known lat/lon
      REAL             :: knownj   ! Y-location of known lat/lon
181      REAL             :: re_m     ! Radius of spherical earth, meters
181      REAL             :: re_m     ! Radius of spherical planet, meters
...
231      proj%re_m     = EARTH_RADIUS_M
231      proj%re_m     = MARS_RADIUS_M
```

./metgrid/src/read_met_module.F 修改内容：
```fortran
143         fg_data % earth_radius = EARTH_RADIUS_M / 1000.
143         fg_data % earth_radius = MARS_RADIUS_M / 1000.
...
237         fg_data % earth_radius = EARTH_RADIUS_M / 1000.
237         fg_data % earth_radius = MARS_RADIUS_M / 1000.
```

./metgrid/src/module_date_pack.F
![alt text](image.png)
```fortran
      ! -------------------------------------------------------------------------
      ! 源代码
      !  Check that the month of NDATE makes sense.
      
      IF ((monew.GT.12).or.(monew.LT.1)) THEN
         PRINT*, 'GETH_IDTS:  Month of NDATE = ', monew
      !
      ! -------------------------------------------------------------------------
      ! 新代码
      !  In planetary dates, the five-digit field is the 1-based sol of year.
      IF ((dynew.GT.MARS_YEAR_SOLS).or.(dynew.LT.1)) THEN
         PRINT*, 'GETH_IDTS:  Sol of NDATE = ', dynew
      ! -------------------------------------------------------------------------
         npass = .false.
      END IF
      
      ! -------------------------------------------------------------------------
      ! 源代码
      !  Check that the month of ODATE makes sense.
      
      IF ((moold.GT.12).or.(moold.LT.1)) THEN
         PRINT*, 'GETH_IDTS:  Month of ODATE = ', moold
      ! -------------------------------------------------------------------------
      ！
      ! -------------------------------------------------------------------------
      ！ 新代码
      IF ((dyold.GT.MARS_YEAR_SOLS).or.(dyold.LT.1)) THEN
         PRINT*, 'GETH_IDTS:  Sol of ODATE = ', dyold
      ! -------------------------------------------------------------------------
         opass = .false.
      END IF
      
      ! -------------------------------------------------------------------------
      ! 下面这一段要删掉
      !  Check that the day of NDATE makes sense.
      
      IF (monew.ne.2) THEN
      ! ...... For all months but February
         IF ((dynew.GT.mday(monew)).or.(dynew.LT.1)) THEN
            PRINT*, 'GETH_IDTS:  Day of NDATE = ', dynew
            npass = .false.
         END IF
      ELSE IF (monew.eq.2) THEN
      ! ...... For February
         IF ((dynew.GT.nfeb(yrnew)).OR.(dynew.LT.1)) THEN
            PRINT*, 'GETH_IDTS:  Day of NDATE = ', dynew
            npass = .false.
         END IF
      END IF
      
      !  Check that the day of ODATE makes sense.
      
      IF (moold.ne.2) THEN
      ! ...... For all months but February
         IF ((dyold.GT.mday(moold)).or.(dyold.LT.1)) THEN
            PRINT*, 'GETH_IDTS:  Day of ODATE = ', dyold
            opass = .false.
         END IF
      ELSE IF (moold.eq.2) THEN
      ! ....... For February
         IF ((dyold.GT.nfeb(yrold)).or.(dyold.LT.1)) THEN
            PRINT*, 'GETH_IDTS:  Day of ODATE = ', dyold
            opass = .false.
         END IF
      END IF
      ! 删除到此
      ! -------------------------------------------------------------------------

      ! -------------------------------------------------------------------------
      ! 源代码
         newdys = newdys + (365 + (nfeb(i)-28))
      ! -------------------------------------------------------------------------
      ！
      ! -------------------------------------------------------------------------
      ! 新代码
         newdys = newdys + MARS_YEAR_SOLS
      ! -------------------------------------------------------------------------
      ！
      END DO

      !
      ! -------------------------------------------------------------------------
      ! 下面这一段要删掉
      IF (monew .GT. 1) THEN
         mday(2) = nfeb(yrnew)
         DO i = 1, monew - 1
            newdys = newdys + mday(i)
         END DO
         mday(2) = 28
      END IF
      ! 删除到此
      ! -------------------------------------------------------------------------
      !
      ! -------------------------------------------------------------------------
      ! 下面这一段要删掉
      IF (moold .GT. 1) THEN
         mday(2) = nfeb(yrold)
         DO i = 1, moold - 1
            olddys = olddys + mday(i)
         END DO
         mday(2) = 28
      END IF
      ! 删除到此
      ! -------------------------------------------------------------------------

      ! 修改
      ! -------------------------------------------------------------------------
      ! 原内容
      !  From old date ('YYYY-MM-DD HH:MM:SS.ffff') and 
      ! -------------------------------------------------------------------------
      ! 新内容
      !  From old MarsWRF date ('YYYY-DDDDD HH:MM:SS.ffff') and
      ! -------------------------------------------------------------------------


      ! 修改
      ! -------------------------------------------------------------------------
      ! 原内容
      READ(odate(6:7),  '(I2)') moold
      READ(odate(9:10), '(I2)') dyold
      ! -------------------------------------------------------------------------
      ! -------------------------------------------------------------------------
      ! 新内容
      READ(odate(6:10), '(I5)') dyold
      moold = 0
      ! -------------------------------------------------------------------------

      ! -------------------------------------------------------------------------
      ! 原内容
      !  Set the number of days in February for that year.
      
      mday(2) = nfeb(yrold)

      ... 

      !  Check that ODATE makes sense.
      
      opass = .TRUE.
      
      !  Check that the month of ODATE makes sense.
      
      IF ((moold.GT.12).or.(moold.LT.1)) THEN
         WRITE(*,*) 'GETH_NEWDATE:  Month of ODATE = ', moold
         opass = .FALSE.
      END IF
      
      !  Check that the day of ODATE makes sense.
      
      IF ((dyold.GT.mday(moold)).or.(dyold.LT.1)) THEN
         WRITE(*,*) 'GETH_NEWDATE:  Day of ODATE = ', dyold
      ! -------------------------------------------------------------------------
      ! 
      ! -------------------------------------------------------------------------
      ! 新内容
      !  Check that the sol-of-year of ODATE makes sense.
      IF ((dyold.GT.MARS_YEAR_SOLS).or.(dyold.LT.1)) THEN
         WRITE(*,*) 'GETH_NEWDATE:  Sol of ODATE = ', dyold
      ! -------------------------------------------------------------------------
      !

      ! -------------------------------------------------------------------------
      ! 原内容
            IF (dynew.GT.mday(monew)) THEN
               dynew = dynew - mday(monew)
               monew = monew + 1
               IF (monew .GT. 12) THEN
                  monew = 1
                  yrnew = yrnew + 1
                  ! If the year changes, recompute the number of days in February
                  mday(2) = nfeb(yrnew)
               END IF
      ! -------------------------------------------------------------------------
      ! 
      ! -------------------------------------------------------------------------
      ! 新内容
            IF (dynew.GT.MARS_YEAR_SOLS) THEN
               dynew = dynew - MARS_YEAR_SOLS
               yrnew = yrnew + 1
      ! -------------------------------------------------------------------------
      !

      ! -------------------------------------------------------------------------
      ! 原内容
               monew = monew - 1
               IF (monew.eq.0) THEN
                  monew = 12
                  yrnew = yrnew - 1
                  ! If the year changes, recompute the number of days in February
                  mday(2) = nfeb(yrnew)
               END IF
      ! -------------------------------------------------------------------------
      ! 
      ! -------------------------------------------------------------------------
      ! 新内容
               dynew = mday(monew)
               dynew = MARS_YEAR_SOLS
               yrnew = yrnew - 1

      ! -------------------------------------------------------------------------
      !

      nlen = LEN(ndate)
      
      IF (nlen.GT.20) THEN
      ! -------------------------------------------------------------------------
      ! 原内容
         WRITE(ndate(1:19),19) yrnew, monew, dynew, hrnew, minew, scnew
      ! -------------------------------------------------------------------------
      ! 
      ! -------------------------------------------------------------------------
      ! 新内容
         WRITE(ndate(1:19),19) yrnew, dynew, hrnew, minew, scnew
      ! -------------------------------------------------------------------------
      !

         WRITE(hfrc,'(I10)') frnew+1000000000
         ndate = ndate(1:19)//'.'//hfrc(31-nlen:10)
      
      ELSE IF (nlen.eq.19.or.nlen.eq.20) THEN
         WRITE(ndate(1:19),19) yrnew, monew, dynew, hrnew, minew, scnew
      19   format(I4,'-',I2.2,'-',I2.2,'_',I2.2,':',I2.2,':',I2.2)
      ! -------------------------------------------------------------------------
      ! 新内容
         WRITE(ndate(1:19),19) yrnew, dynew, hrnew, minew, scnew
      19   format(I4.4,'-',I5.5,'_',I2.2,':',I2.2,':',I2.2)
      ! -------------------------------------------------------------------------
      !

         IF (nlen.eq.20) ndate = ndate(1:19)//'.'
      
      ELSE IF (nlen.eq.16) THEN
         WRITE(ndate,16) yrnew, monew, dynew, hrnew, minew
      16   format(I4,'-',I2.2,'-',I2.2,'_',I2.2,':',I2.2)
      ! 
      ! -------------------------------------------------------------------------
      ! 新内容
         WRITE(ndate,16) yrnew, dynew, hrnew, minew
      16   format(I4.4,'-',I5.5,'_',I2.2,':',I2.2)
      ! -------------------------------------------------------------------------
      !

      ELSE IF (nlen.eq.13) THEN
         WRITE(ndate,13) yrnew, monew, dynew, hrnew
      13   format(I4,'-',I2.2,'-',I2.2,'_',I2.2)
      ! 
      ! -------------------------------------------------------------------------
      ! 新内容
         WRITE(ndate,13) yrnew, dynew, hrnew
      13   format(I4.4,'-',I5.5,'_',I2.2)
      ! -------------------------------------------------------------------------
      !
      
      ELSE IF (nlen.eq.10) THEN
         WRITE(ndate,10) yrnew, monew, dynew
      10   format(I4,'-',I2.2,'-',I2.2)
      ! 
      ! -------------------------------------------------------------------------
      ! 新内容
         WRITE(ndate,10) yrnew, dynew
      10   format(I4.4,'-',I5.5)
      ! -------------------------------------------------------------------------
      !
      END IF

      ! -------------------------------------------------------------------------
      ! 原内容
      READ(date,FMT='( 5X,I2.2)') month
      READ(date,FMT='( 8X,I2.2)') day
      ! -------------------------------------------------------------------------
      ! 
      ! -------------------------------------------------------------------------
      ! 新内容
      month = 0
      READ(date,FMT='( 5X,I5.5)') day
      ! -------------------------------------------------------------------------
      !
```


2. 把 WRFV3 `share/module_date_time.F` 的 `WRF_PLANET` 五位 sol 解析、日期差和跨年逻辑移植到 WPS 的日期模块。

否则会出现两类不同问题：geogrid 的米制网格属性按地球半径计算；metgrid 把 `0028-00507` 解析成 00 月 07 日。

## 2. 制作静态 tile

需要将已有 Mars 数据转换为 WPS geogrid binary tile，而不是直接改文件名：

```text
MOLA/TES 原文件
  -> 解码字节序、维度、比例因子和缺测值
  -> 统一到 0..360 经度和南到北纬度顺序
  -> 转成 index.copy 所描述的 1 度网格，或更新 index 描述实际网格
  -> 写出类似 00001-00360.00001-00180 的 tile
```

全陆面与单一土壤分类 tile 可以人工构造为常数 1；物理字段不得在没有说明的情况下用地球数据代替。

## 3. 运行名称

跑WPS之前要准备的东西就是这些：

```text
namelist.wps
geogrid/GEOGRID.TBL
WPS_GEOG_MARS
./geogrid.exe
```

## 4. 静态检查重点

- `namelist.wps` 中不应再有全角逗号或全角感叹号。
- `max_dom=1`、`e_we=73`、`e_sn=37` 应与 MarsWRF 单域和 MACDA 72 x 36 网格对应。
- intermediate 文件名、`prefix` 和 `fg_name` 都应使用 `MACDA`。
- `geo_em.d01.nc` 的 mass grid 应为 72 x 36，经度为 0..355，纬度为 -87.5..87.5。
- `HGT_M` 单位应为 m；`ALBEDO/ALBBCK` 和 `EMISS/EMBCK` 应为 0..1。
- 当前 real 兼容路径的 `ALBEDO12M` 应为百分数并有 12 层；不能把 0..1 数据交给随后除以 100 的代码。
- `THC/THCBCK` 数值和单位必须与 MarsWRF 实际读取逻辑核对。
- `LANDMASK` 应全为 1，不应引入地球海洋或地球土地利用类别。
- NetCDF 全局属性中的半径、`DX` 和 `DY` 应对应火星，不是 6370 km 地球半径。
- 时间属性应保留 `0028-00507_02:00:00`，不能被改回伪造的地球日期。

## 5. 下游检查重点

即使 geogrid 输出正确，也要检查：

- metgrid 是否认识五位 sol 并能调度 `MACDA:0028-00507_02`。
- `METGRID.TBL` 是否从 `PRESSURE` 派生 `PRES`，并传递 `TT/UU/VV/GHT/PSFC/SKINTEMP`。
- `real.exe` 是否把 `ALBEDO/ALBBCK`、`EMISS/EMBCK`、`THC/THCBCK` 带入 `wrfinput_d01`。
- `real.exe` 是否错误地对火星反照率再除以 100。
- `wrfinput_d01` 的 `PLANET_YEAR`、`P2SI`、`Times`、网格半径和静态字段是否一致。
